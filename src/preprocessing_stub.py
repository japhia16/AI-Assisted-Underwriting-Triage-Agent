def preprocess_submission(submission: dict) -> dict:
    """
    Placeholder for Student 1's reusable preprocessing function.
    TODO: replace this import with the real preprocess_submission from
    Student 1's module once delivered. Signature must stay: dict in, dict out.
    For now this is a no-op passthrough so the integration seam exists and
    downstream code can be written/tested against it today.
    """
    return submission
